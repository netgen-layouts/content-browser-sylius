item_types:
    sylius_product:
        name: item_types.sylius_product
        preview:
            template: '%netgen_content_browser.sylius_product.item_template%'
        columns:
            name:
                name: columns.name
                value_provider: name
            price:
                name: columns.sylius_product.price
                template: '%netgen_content_browser.sylius_product.price_template%'
            product_id:
                name: columns.sylius_product.product_id
                value_provider: sylius_product\product_id
        default_columns:
            - name
            - price

    sylius_taxon:
        name: item_types.sylius_taxon
        preview:
            template: '%netgen_content_browser.sylius_taxon.item_template%'
        columns:
            name:
                name: columns.name
                value_provider: name
            product_id:
                name: columns.sylius_taxon.taxon_id
                value_provider: sylius_taxon\taxon_id
        default_columns:
            - name
            - taxon_id
